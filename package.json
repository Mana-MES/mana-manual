{
  "name": "mana-manual-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/inject-seo.js && node scripts/optimize-images.js",
    "preview": "vite preview",
    "ngrok": "ngrok http 5173"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "vite": "^6.0.1",
    "vite-plugin-static-copy": "^2.1.0"
  }
}
